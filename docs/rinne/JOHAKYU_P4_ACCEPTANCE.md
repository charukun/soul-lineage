# P4 — canonical technique identity and authored motion bindings

Source: P3 accepted head `76981bcfee5df542acf67497bd9e17cf8d641516`, including develop `063b31b39820f635f8686c27f88589914fe984a0`.

The existing executor now publishes its actual attack instance, committed recipe, phase, step, target and motion progress. Soft policy changes cannot rename an in-flight attack. Stamina charges are keyed by actual attack instance rather than a repeated kind label. Rinne canonical frames and contact events consume these fields. Read-only diagnostics distinguish basic, learned, equipped and trial states; seeing a trial cannot commit an inspiration or invent a technique.

`?johakyu=p4` is an explicit sword catalog-trial review using the existing Nocturne motor and the shared canonical physiology. It does not save or grant equipment. Seven canonical weapon families bind existing authored clips. Unsupported actions or absent clips fail closed; this is not a claim that every generated recipe has a verified visual binding. Full equipment realization and main-game presentation are P5–P7 work.

The original renderer, asset manifest, native motor timings, camera, lighting, audio and frozen BK remain unchanged. Phase verification includes native runtime execution, source/target identities, policy continuity, learned/trial isolation, authored clip existence, original baseline invariants, canonical inspiration regressions, and review/rinne builds through Astra. Pixel Fold measurements and visual acceptance are not inferred from these tests.

Rollback: remove the P4 adapter and shared facade metadata as one integration unit; no save schema or learned-skill migrations were added. P2/P3 query modes remain available.
