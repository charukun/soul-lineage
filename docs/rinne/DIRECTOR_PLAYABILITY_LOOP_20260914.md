# 輪廻転焦 本編ディレクター改善ループ

2026-09-14。最新developを正本に、輪廻転焦本編だけを実際に操作し、ゲームとして遊びを阻害する問題を優先順に修正する。UIの見た目だけを目的にせず、移動・生活・成長・前線・技・休息・保存再開までの一連の体験を対象にする。

## 今回の境界

- 対象: `apps/rinne` 本編、必要な本編局所テストと文書。
- 現行のLifeClock、スワイプ/フリック、自動戦闘、序破急、意識バランス、MURAAAAAAA共有配置、保存/転生の既存仕様を優先する。
- Character Workshop / Visual Review Labはレビュー用ツールとして維持し、本編UIへ管理画面を持ち込まない。
- 武器構え・肘/肩/手首の人体姿勢はPR #156でdevelopへ統合済み。今回branchではモーション補正を重複実装しない。
- 通常UIのスクロール禁止・手帳ページ化はPR #158でdevelopへ統合済み。今回の再統合ではそのページ操作と回帰テストを正本として残す。
- main / Productionは変更しない。

## ディレクター受入条件

1. 390×844のスマートフォン相当表示で、出生から歩行、生活行動、休息、地図、技・装備、前線、救助、帰村まで主要ループを通常入力で進められる。
2. プレイヤーが「今なにをするか」「次にどこへ行くか」「行動すると何が変わったか」を画面から判断できる。説明文だけで操作対象を探させない。
3. 移動・休息・生活行動・戦闘・メニュー中の状態が矛盾しない。メニューや手帳を閉じた後は、元のsolo停止状態を尊重して入力へ復帰する。
4. 主操作と戦闘/生活フィードバックが互いを遮らない。タッチ対象は原則44px以上で、画面中央の移動領域を確保する。
5. 成長・生活経験・技発見・前線突破・救助・転生の変化が、少なくとも次の判断に必要な範囲で直後に伝わる。
6. 保存再開、MURA配置更新、既存技/装備継承を壊さない。
7. 実ブラウザ回帰はテスト専用のゲーム状態setterやforce clickで成功させない。必要な長寿命シナリオのfixture読込は保存Envelope経由に限定する。
8. 「十分」の終了条件は、主要ループを再操作して新しいP0/P1の操作停止・意味不明な導線・状態不整合が見つからず、残りがコンテンツ拡張/審美調整として別フェーズに切り分けられること。

## 今回見つけて直した点

- DOM追加順に依存していた主行動を、現在の意図に沿って優先する。生活行動中の中断、受取待ちの閃き、救助、前線進行、帰還/出航を、一般的な周辺行動より上に出す。可否判定そのものは既存controllerを正本とする。
- 生活行動中や閃き待ちの短期目標を上部の目的表示へ反映し、「次に何が変わるか」を現在状態と一致させる。
- 同じ焚き火座標へfallbackしている複数の生活施設を地図上で別施設のように重複表示せず、一つの目的地へまとめて可能な行動を列挙する。既存`garden` IDをcanonicalとして保存・導線互換を維持する。
- カメラと主人公の間に大型施設が近接した場合だけ、その施設の表示materialを局所的にフェードし、主人公と移動方向を見失いにくくする。world geometry、当たり判定、配置、保存は変更しない。

## 検証経過

- `storyActionPriority` / 短期目標 / 地図目的地統合をpure functionとして局所テスト化。
- 390×844の専用review run `34787080281` は成功し、本編通常画面と地図を実描画で確認。地図は焚き火fallbackを1項目へ集約し、主要4目的地として表示された。
- review後にdevelopへPR #156 / #158が統合されたため、古いbaseのgreenを最終証拠には流用せず、最新developへ載せ直して通常CI/browser gateを再実行する。

## Delivery

通常gitを第一経路として試行し、DNS失敗のため接続済みGitHub APIへ切替済み。同一branchを継続する。Draft PRで実装し、局所/fast検証、Ready化、既存Integration、develop統合、DEV公開、公開sourceとfocused browser証拠まで確認する。CI条件・browser assertion・承認を期限のために弱めない。
