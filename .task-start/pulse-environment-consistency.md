# PULSE / application environment consistency

- Rename the operations board to PULSE without changing its stable Worker URL.
- Remove explanatory parentheses from Visual Review Lab and WAYFINDER.
- Reconcile the demon display title with 尽喰廻遊 across current metadata and publication views; preserve technical IDs and save-data keys.
- Audit development, staging and production for each game. Add missing deployment support and show missing/unverified targets honestly rather than marking configured URLs as published.
- Preserve published production bytes unless an explicit promotion is performed. Follow the existing Draft -> Ready -> Integration flow.
