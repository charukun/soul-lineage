# Meshy-based protagonist / Shino candidate

Task: use the public Meshy chibi female-adventurer candidate as the visible modeling base, reshape it toward the current RINNE protagonist and Shino visual direction, and rebuild it on a clean CC0/RINNE-owned runtime rig path.

Acceptance for this DCC pass:

- do not reuse the retired Sendagaya_Shino-derived runtime/carrier asset;
- preserve the Meshy source mesh/material identity where practical instead of replacing it with Three.js/procedural primitives;
- record the exact Meshy page/model identifier, declared license, fetched source SHA-256/bytes, and the clean KayKit rig provenance in generated evidence;
- generate an editable Blender source, GLB candidate, and fixed front / three-quarter / side / back review captures through Character DCC Carrier;
- keep `visualApproval=pending` and `productionReady=false`; do not promote beyond evidence actually produced;
- do not modify `main` / Production.

The current runtime hero on `develop` is still the direct KayKit Rogue selection. The existing DCC protagonist candidate is the Knight-head/arms/legs + Rig_Medium / Rogue_Body reconstruction in PR #584; it is the comparison baseline, not the source asset for this Meshy pass.
