import { TITLE_VIDEO_URL, TITLE_POSTER_URL } from './title-cinematic-media.js';
export function createTitleCinematicController(options){return new TitleCinematicController(options);}
class TitleCinematicController{
 constructor({title,video,motionToggle,motionKey,getPrepared=()=>null,resetParallax=()=>{}}){this.title=title;this.video=video;this.motionToggle=motionToggle;this.motionKey=motionKey;this.getPrepared=getPrepared;this.resetParallax=resetParallax;this.reducedMotion=window.matchMedia?.('(prefers-reduced-motion: reduce)');this.introPlayed=false;this.realtime=false;this.skipReady=false;this.timers=[];this.syncMotion=this.syncMotion.bind(this);}
 prefersReducedMotion(){return Boolean(this.reducedMotion?.matches);}
 clear(){for(const t of this.timers)clearTimeout(t);this.timers=[];}
 pause(){this.video.pause();}
 activateFallback(reason){if(reason)console.warn('Realtime title fallback',reason);this.clear();this.pause();this.realtime=false;this.skipReady=false;this.title.dataset.media='fallback';this.title.dataset.intro='idle';this.title.dataset.skip='done';this.getPrepared()?.startTitlePreview?.({cinematic:false});}
 setSkipReady(){if(!this.title.hidden&&this.title.dataset.intro==='cinematic'){this.skipReady=true;this.title.dataset.skip='ready';}}
 settleUi({skipped=false}={}){if(this.title.hidden||this.title.dataset.intro!=='cinematic')return;this.clear();this.skipReady=false;this.title.dataset.skip=skipped?'requested':'done';this.title.dataset.intro='settling';this.getPrepared()?.startTitlePreview?.({cinematic:false});this.timers.push(setTimeout(()=>{if(!this.title.hidden){this.title.dataset.intro='idle';this.title.dataset.skip='done';}},900));}
 startRealtime(){const prepared=this.getPrepared();if(!prepared||this.title.hidden)return false;this.clear();this.pause();this.realtime=true;this.introPlayed=true;this.skipReady=false;this.title.dataset.media='realtime';this.title.dataset.intro='cinematic';this.title.dataset.skip='locked';prepared.startTitlePreview?.({cinematic:true,lowResolution:true});this.timers.push(setTimeout(()=>this.setSkipReady(),1500));this.timers.push(setTimeout(()=>this.settleUi(),8200));return true;}
 skip(){if(this.title.dataset.intro!=='cinematic'||!this.skipReady)return false;this.settleUi({skipped:true});return true;}
 begin(){if(this.introPlayed){if(this.title.dataset.intro==='cinematic'||this.title.dataset.intro==='settling')return;this.title.dataset.intro='idle';this.title.dataset.skip='done';this.getPrepared()?.startTitlePreview?.({cinematic:false});return;}if(this.title.dataset.motion!=='on'||this.prefersReducedMotion()){this.introPlayed=true;this.title.dataset.intro='idle';this.title.dataset.media='fallback';this.title.dataset.skip='done';this.getPrepared()?.startTitlePreview?.({cinematic:false});return;}if(this.startRealtime())return;this.title.dataset.intro='pending';this.title.dataset.media='fallback';this.title.dataset.skip='locked';}
 onPrepared(){if(!this.realtime&&this.title.dataset.motion==='on'&&!this.prefersReducedMotion())this.startRealtime();}
 syncMotion(){if(this.title.hidden)return;const allowed=this.title.dataset.motion==='on'&&!this.prefersReducedMotion();if(!allowed){this.clear();this.pause();this.realtime=false;this.introPlayed=true;this.title.dataset.intro='idle';this.title.dataset.media='fallback';this.title.dataset.skip='done';this.getPrepared()?.startTitlePreview?.({cinematic:false});this.resetParallax();return;}if(this.title.dataset.intro==='idle')this.getPrepared()?.startTitlePreview?.({cinematic:false});}
 setMotion(enabled,persist=false){this.title.dataset.motion=enabled?'on':'off';this.motionToggle.setAttribute('aria-checked',String(enabled));const state=this.motionToggle.querySelector('.setting-switch-state');if(state)state.textContent=enabled?'入':'切';if(!enabled)this.resetParallax();if(persist){try{localStorage.setItem(this.motionKey,enabled?'on':'off');}catch{}}this.syncMotion();}
 init(){this.video.muted=true;this.video.playsInline=true;this.video.poster=TITLE_POSTER_URL;this.video.src=TITLE_VIDEO_URL;this.reducedMotion?.addEventListener?.('change',this.syncMotion);let initial=!this.prefersReducedMotion();try{const stored=localStorage.getItem(this.motionKey);if(!this.prefersReducedMotion()&&(stored==='on'||stored==='off'))initial=stored==='on';}catch{}this.setMotion(initial);}
 dispose(){this.clear();this.pause();this.reducedMotion?.removeEventListener?.('change',this.syncMotion);}
}