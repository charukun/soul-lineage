import test from 'node:test';
import assert from 'node:assert/strict';
import {readFile} from 'node:fs/promises';

test('Character Workshop loads the Motion QA debug overlay and controls',async()=>{const html=await readFile(new URL('../characters.html',import.meta.url),'utf8'),source=await readFile(new URL('../src/character-motion-debug.js',import.meta.url),'utf8');assert.match(html,/character-motion-debug\.js/);assert.match(source,/motion-debug-overlay/);assert.match(source,/motion-debug-summary/);assert.match(source,/motion-debug-controls/);assert.match(source,/window\.masterCharacterReview/);assert.match(source,/qa\?\.active/);for(const key of ['com','support','semantic','future','footLocks','weaponArc','interaction','hit','warp','pose','layers'])assert.match(source,new RegExp(`${key}:true`));});
test('overlay visualizes real COM/support/semantic and consumes runtime evidence without inventing it',async()=>{const source=await readFile(new URL('../src/character-motion-debug.js',import.meta.url),'utf8');assert.match(source,/estimateCenterOfMass/);assert.match(source,/leftFoot/);assert.match(source,/rightFoot/);assert.match(source,/createSemanticMotionTimeline/);assert.match(source,/authored-slash\.js/);assert.match(source,/__HUMANOID_LAB__/);assert.match(source,/motionWarp/);assert.match(source,/Pose Search/);assert.match(source,/Weapon Arc: N\/A（実tip evidence未入力）/);assert.doesNotMatch(source,/actor\.(?:x|z|yaw)\s*=/,'debug overlay must not write actor authority');});
