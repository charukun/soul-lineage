import {sourceURL} from './assets.mjs';
// Authored HUD layout is the user's UI-only exception. Glyphs remain sourced SVGs.
const aliases={'magic-slot':'sparkles','potion-red':'flask-conical','potion-green':'heart'};
export const uiIcon=name=>`<img class="ui-icon" src="${sourceURL('uiIcons',(aliases[name]||name)+'.svg')}" alt="" draggable="false">`;
export const menuItems=[['journal','手帳','book-open'],['bag','かばん','backpack'],['forge','工房','hammer'],['map','地図','map'],['housing','自宅','house'],['settings','設定','settings-2']];
export function hudTemplate(game,esc,button){
 const skillIcons={arc:'sword',heal:'heart',nova:'sparkles',bolt:'crosshair',storm:'moon',novaheal:'shield'};
 return `<div class="hud-shade"></div><section class="hud" aria-label="旅人の状態"><div class="crest">${uiIcon('sword')}</div><div class="hud-detail"><div class="identity"><strong>${esc(game.save.name)}</strong><span id="level"></span></div><div class="gauge" aria-label="HP"><span id="hp"></span></div><div class="gauge mana" aria-label="MP"><span id="mp"></span></div><div class="stats"><span id="vitals"></span><span id="gold"></span></div></div></section>${button('menu',uiIcon('menu'),'id="menu-toggle" aria-label="メニューを開く" aria-expanded="false" aria-controls="game-menu"','round menu-toggle')}<nav id="game-menu" class="nav glass-panel" aria-label="旅のメニュー" hidden>${menuItems.map(([a,n,i])=>button(a,uiIcon(i)+`<span>${n}</span>`)).join('')}</nav><button class="quest" data-action="journal" aria-label="旅の手帳を開く"><span class="quest-rule"></span><span><small>STORY</small><strong id="quest-title"></strong><span id="quest-desc"></span></span>${uiIcon('chevron-right')}</button><section class="place">${uiIcon('moon')}<span id="place"></span></section><div id="joystick" aria-hidden="true"><div id="stick"></div></div><div class="skills" aria-label="スキル">${button('attack',uiIcon('sword')+'<span>攻撃</span>','id="manual" aria-label="通常攻撃"','round manual')}${game.job.skills.map((s,i)=>button('skill:'+i,`${uiIcon(skillIcons[s[1]]||'sparkles')}<span>${s[0]}</span><small>${s[3]} MP</small><b id="cool${i}" class="cooldown" hidden></b>`,`aria-label="${s[0]} / ${s[3]} MP"`,'round skill skill-'+i)).join('')}${button('potion',uiIcon('flask-conical')+'<span id="potions"></span>','aria-label="灯の雫で回復"','round potion')}${button('auto','AUTO','id="auto" aria-label="接近自動戦闘を切り替え"','auto')}</div>${button('interact','話す','id="interact" hidden','interact')}${button('camera-reset',uiIcon('compass'),'aria-label="視点を正面に戻す"','round camera-reset')}<span id="save-status" class="sr-only" aria-live="off"></span><div id="target-name" class="target-name" hidden></div>`;
}
export function titleTemplate(saved,button){return `<div class="brand">LANTERNFELL <span>夜灯の章</span></div><section class="title-card"><p class="eyebrow">A LITTLE LIGHT. A LONG JOURNEY.</p><h1>灯渡りの庭</h1><p class="title-copy">夜の向こうに、<br>帰る場所を灯そう。</p>${saved?button('continue','旅をつづける','','primary'):''}${button('new',saved?'新しい旅人':'庭へ旅立つ','',saved?'quiet':'primary')}</section><div class="title-bottom">${button('credits','素材の出典','','quiet')}</div>`;}
