const clamp=(x,a=0,b=1)=>Math.max(a,Math.min(b,x));
const finite=(...v)=>v.every(Number.isFinite);
const xyz2=p=>Array.isArray(p)?{x:p[0],z:p[1]??p[2]??0}:p;
const angleDelta=(from,to)=>{let d=(to-from)%(Math.PI*2);if(d>Math.PI)d-=Math.PI*2;if(d<-Math.PI)d+=Math.PI*2;return d;};
const hash=value=>{let h=2166136261;for(const c of String(value)){h^=c.charCodeAt(0);h=Math.imul(h,16777619);}return h>>>0;};
const unit=n=>(n>>>0)/4294967295;
export const LOCAL_LAYER_ORDER=['source-pose','body-adaptation','personality','condition','locomotion-intent','interaction','ground-contact','weapon-sockets','secondary','qa'];
export function localLayerPlan(){return{version:1,order:[...LOCAL_LAYER_ORDER],presentationOnly:true};}
export function localSemanticTimeline({duration,contactPhase=.5,activeStart=.42,activeEnd=.58,plantPhase=.12,handoffPhase=.9}={}){if(!finite(duration,contactPhase,activeStart,activeEnd,plantPhase,handoffPhase)||duration<=0||activeStart>contactPhase||contactPhase>activeEnd)throw Error('Invalid semantic motion timeline');const phases={'foot-plant':plantPhase,'weight-transfer':clamp((plantPhase+activeStart)*.5),'anticipation-end':activeStart,'weapon-release':clamp(activeStart+(contactPhase-activeStart)*.38),contact:contactPhase,'follow-through':clamp(activeEnd+(1-activeEnd)*.32),handoff:Math.max(activeEnd,handoffPhase)};return{version:1,duration,contactPhase,activeStart,activeEnd,events:Object.entries(phases).map(([name,phase])=>({name,phase,seconds:phase*duration,authoritative:false})).sort((a,b)=>a.phase-b.phase||a.name.localeCompare(b.name)),contactAuthority:'gameplay-external'};}
export function localPredict({position={x:0,z:0},velocity={x:0,z:0},intent={x:0,z:0},yaw=0,horizon=.45,steps=4,maxAcceleration=7,maxSpeed=6}={}){const p=xyz2(position),v=xyz2(velocity),i=xyz2(intent);if(!p||!v||!i||!finite(p.x,p.z,v.x,v.z,i.x,i.z,yaw,horizon,steps,maxAcceleration,maxSpeed)||horizon<=0||horizon>.75||!Number.isInteger(steps)||steps<2||steps>8)throw Error('Invalid predictive trajectory');let x=p.x,z=p.z,vx=v.x,vz=v.z;const dt=horizon/steps,mag=Math.hypot(i.x,i.z),targetSpeed=Math.min(maxSpeed,mag),tx=mag>1e-9?i.x/mag*targetSpeed:0,tz=mag>1e-9?i.z/mag*targetSpeed:0,points=[];for(let n=1;n<=steps;n++){let ax=tx-vx,az=tz-vz,am=Math.hypot(ax,az),limit=maxAcceleration*dt;if(am>limit){ax*=limit/am;az*=limit/am;}vx+=ax;vz+=az;const sm=Math.hypot(vx,vz);if(sm>maxSpeed){vx*=maxSpeed/sm;vz*=maxSpeed/sm;}x+=vx*dt;z+=vz*dt;points.push({t:n*dt,x,z,vx,vz});}return{version:1,origin:{x:p.x,z:p.z},yaw,horizon,points,advisory:true};}
function trajectoryError(query,candidate){const qa=query.trajectory?.points??query.trajectory??[],ca=candidate.trajectory?.points??candidate.trajectory??[];if(!qa.length||!ca.length)return 0;const count=Math.min(qa.length,ca.length);let sum=0;for(let n=0;n<count;n++){const q=xyz2(qa[n]),c=xyz2(ca[n]);sum+=Math.hypot((q?.x??0)-(c?.x??0),(q?.z??0)-(c?.z??0));}return sum/count;}
export function localRank({query,candidates,weights={trajectory:1.6,speed:.8,facing:.7,support:.45,state:.65,continuity:.55}}={}){if(!query||!Array.isArray(candidates)||!candidates.length)throw Error('Invalid pose search');return candidates.map(candidate=>{const speed=Math.abs((Number(candidate.speed)||0)-(Number(query.speed)||0)),facing=Math.abs(angleDelta(Number(query.yaw)||0,Number(candidate.yaw)||0)),support=query.supportSide&&candidate.supportSide&&query.supportSide!==candidate.supportSide?1:0,state=query.state&&candidate.state&&query.state!==candidate.state?1:0,continuity=1-clamp(Number.isFinite(candidate.continuity)?candidate.continuity:0),trajectory=trajectoryError(query,candidate),score=trajectory*(weights.trajectory??1)+speed*(weights.speed??1)+facing*(weights.facing??1)+support*(weights.support??1)+state*(weights.state??1)+continuity*(weights.continuity??1);return{id:String(candidate.id),score,terms:{trajectory,speed,facing,support,state,continuity},candidate};}).sort((a,b)=>a.score-b.score||a.id.localeCompare(b.id));}
export function localVariation(seed){const h=hash(seed),r=salt=>unit(hash(`${h}:${salt}`));return{version:1,seed:String(seed),startFoot:r('foot')<.5?'left':'right',idlePhase:r('idle'),recoveryAmplitude:.92+r('recovery')*.16,stanceScale:.94+r('stance')*.12,turnScale:.92+r('turn')*.16,gaitPhase:r('gait'),contactTimingScale:1,authoritativeTimingChanged:false};}
const SCHEMAS={'talk-close':{anchors:['head','head'],distance:[.75,1.45],maxYaw:.8,release:'handoff'},handshake:{anchors:['rightHand','rightHand'],distance:[.6,1.15],maxYaw:.65,release:'handoff'},hug:{anchors:['chest','chest'],distance:[.45,1],maxYaw:.55,release:'handoff'},'carry-child':{anchors:['chest','hips'],distance:[.15,.8],maxYaw:.65,release:'handoff'},grab:{anchors:['rightHand','chest'],distance:[.3,.9],maxYaw:.75,release:'handoff'},'blade-clash':{anchors:['weaponTip','weaponTip'],distance:[.55,1.65],maxYaw:1.1,release:'follow-through'},'eat-target':{anchors:['mouth','chest'],distance:[.15,.9],maxYaw:.8,release:'handoff'},grapple:{anchors:['chest','chest'],distance:[.25,.9],maxYaw:.7,release:'handoff'}};
export function localSchema(id,{partner,anchors}={}){const schema=SCHEMAS[id];if(!schema||!partner||partner.id==null||!anchors?.self||!anchors?.partner)throw Error('Explicit interaction partner and anchors required');return{version:1,schema:{id,...schema,presentationOnly:true,explicitPartnerRequired:true},partnerId:String(partner.id),anchors:{self:anchors.self,partner:anchors.partner},inferred:false};}
export function localWeaponContact({normal={x:0,z:1},penetration=0,relativeSpeed=0,weaponMass=1,maxDeflection=.14,maxRecoil=.08}={}){const n=xyz2(normal);if(!n||!finite(n.x,n.z,penetration,relativeSpeed,weaponMass,maxDeflection,maxRecoil)||penetration<0||weaponMass<=0)throw Error('Invalid weapon contact constraint');const len=Math.hypot(n.x,n.z)||1,nx=n.x/len,nz=n.z/len,energy=clamp(penetration*3+Math.abs(relativeSpeed)*.025,0,1),deflection=clamp(energy*.12/Math.sqrt(weaponMass),0,maxDeflection),recoil=clamp(energy*.055/weaponMass,0,maxRecoil);return{version:1,normal:{x:nx,z:nz},deflection,recoilOffset:{x:-nx*recoil,z:-nz*recoil},presentationOnly:true,damageAuthority:false};}
