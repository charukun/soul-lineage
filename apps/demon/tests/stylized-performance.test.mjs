import test from 'node:test';
import assert from 'node:assert/strict';
import fs from 'node:fs';
const main=fs.readFileSync(new URL('../src/main.js',import.meta.url),'utf8'),adaptive=fs.readFileSync(new URL('../src/adaptive-visual-performance.js',import.meta.url),'utf8'),resilience=fs.readFileSync(new URL('../src/runtime-resilience.js',import.meta.url),'utf8'),scale=fs.readFileSync(new URL('../src/shared-world-scale.js',import.meta.url),'utf8');
test('demon installs authored LOD, adaptive, resilience and shared-world scale bridges in order',()=>{assert.ok(main.indexOf("./authored-visual-lod.js")<main.indexOf("./stylized-visual-target.js"));assert.ok(main.indexOf("./stylized-visual-target.js")<main.indexOf("./adaptive-visual-performance.js"));assert.ok(main.indexOf("./adaptive-visual-performance.js")<main.indexOf("./runtime-resilience.js"));assert.ok(main.indexOf("./runtime-resilience.js")<main.indexOf("./shared-world-scale.js"));});
test('demon adaptive bridge controls GPU/CPU/thermal costs from learned device capability',()=>{for(const token of ['deviceCapabilityProfile','createGpuAwareQualityGovernor','createGpuTimer','createThermalTrendGovernor','applyVisualQualityFloor','shadowScale','applyTextureQuality','vfxScale','createVisualDistanceStreamer','presentationDistance'])assert.match(adaptive,new RegExp(token));});
test('demon resilience prewarms combat shaders and protects context loss recovery',()=>{for(const token of ['createShaderWarmupManager','createResourceLeakSentinel','installWebGLContextRecovery','installStylizedBakedLighting','installSilhouetteImpostorLOD','view.spark','view.slash'])assert.match(resilience,new RegExp(token.replace('.','\\.')));assert.match(resilience,/context-restored/);assert.match(resilience,/character-ready/);assert.match(resilience,/beforeChildren/);assert.match(resilience,/disposeWarmupMesh/);assert.doesNotMatch(resilience,/game\.(player|village|time)\s*=/);});
test('demon shared-world scale offloads crowd/network/audio planning without changing combat authority',()=>{for(const token of ['createWorldScaleWorker','createCrowdPresenceRenderer','createAssetResidencyCache','audioSummary','runScaleReplay','probeExperimentalWebGPU','__DEMON_WORLD_SCALE__'])assert.match(scale,new RegExp(token));assert.doesNotMatch(scale,/game\.(player|village|time)\s*=/);});
