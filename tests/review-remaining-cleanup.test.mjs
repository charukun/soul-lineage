import test from 'node:test';
import assert from 'node:assert/strict';
import {readFile} from 'node:fs/promises';
const read=path=>readFile(new URL(`../${path}`,import.meta.url),'utf8');
test('Character Studio legacy CSS files are compatibility facades',async()=>{const pairs=[['character-review-main.css','main.css'],['character-review-grid.css','grid.css'],['character-review.css','runtime.css'],['character-review-advanced.css','advanced.css'],['character-review-modular.css','modular.css']];for(const [legacy,canonical] of pairs){assert.equal(await read(`apps/character-studio/src/${legacy}`),`@import './review/character/${canonical}';\n`);}});
test('review runtime canonical paths and shared lifecycle stay in place',async()=>{const [pkg,auto,load,charRuntime,objectRuntime,equipmentRuntime,rinneAuto]=await Promise.all([read('packages/shared-ui/package.json'),read('packages/shared-ui/src/review/auto-install.js'),read('packages/shared-ui/src/review/load-controller.js'),read('apps/character-studio/src/review/character/runtime.js'),read('apps/rinne/src/review/objects/entrypoint.js'),read('apps/rinne/src/review/equipment/entrypoint.js'),read('apps/rinne/src/review/shared/slot-auto.js')]);assert.match(pkg,/review-auto-install/);assert.match(pkg,/review-load-controller/);assert.match(auto,/createReviewAutoInstaller/);assert.match(load,/createReviewLoadController/);for(const source of [charRuntime,objectRuntime,equipmentRuntime])assert.match(source,/createReviewLoadController/);assert.match(charRuntime,/setReviewStatus/);assert.match(rinneAuto,/createReviewAutoInstaller/);});
