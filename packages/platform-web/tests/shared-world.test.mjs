import test from 'node:test';
import assert from 'node:assert/strict';
import {createSharedWorldChannel} from '../src/shared-world.js';
function fake(){const values=new Map(),events=new EventTarget();return{localStorage:{getItem:k=>values.get(k)??null,setItem:(k,v)=>values.set(k,v)},addEventListener:events.addEventListener.bind(events),removeEventListener:events.removeEventListener.bind(events),send(key,newValue){const e=new Event('storage');Object.assign(e,{key,newValue,storageArea:this.localStorage});events.dispatchEvent(e);}};}
const validate=s=>{if(s.version!==1)throw Error('unsupported schema');return structuredClone(s);};
test('only village writer publishes; dev/prod readers stay separate',()=>{const window=fake(),writer=createSharedWorldChannel({environment:'dev',writer:true,validate,window}),reader=createSharedWorldChannel({environment:'dev',validate,window}),prod=createSharedWorldChannel({environment:'prod',validate,window});writer.publish({version:1,id:'mura',revision:3});assert.equal(reader.read().revision,3);assert.equal(prod.read(),null);assert.throws(()=>reader.publish({version:1}));});
test('subscription handles valid updates and preserves current layout on incompatible versions',()=>{const window=fake(),reader=createSharedWorldChannel({environment:'local',validate,window});let current=null,error=null;const stop=reader.subscribe(s=>current=s,e=>error=e);window.send('soul.local.world.mura.v1',JSON.stringify({version:1,revision:4}));assert.equal(current.revision,4);window.send('soul.local.world.mura.v1','{"version":2}');assert.equal(current.revision,4);assert.match(error.message,/schema/);stop();window.send('soul.local.world.mura.v1',JSON.stringify({version:1,revision:5}));assert.equal(current.revision,4);});
