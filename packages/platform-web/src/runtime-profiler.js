class Ring{constructor(capacity){this.values=new Float64Array(capacity);this.cursor=0;this.size=0;}push(value){if(!Number.isFinite(value))return;this.values[this.cursor]=value;this.cursor=(this.cursor+1)%this.values.length;this.size=Math.min(this.values.length,this.size+1);}array(){const out=new Array(this.size),start=(this.cursor-this.size+this.values.length)%this.values.length;for(let i=0;i<this.size;i++)out[i]=this.values[(start+i)%this.values.length];return out;}}
const percentile=(values,p)=>{if(!values.length)return null;const sorted=[...values].sort((a,b)=>a-b);return sorted[Math.min(sorted.length-1,Math.max(0,Math.ceil(sorted.length*p)-1))];};
const summarize=ring=>{const values=ring.array();return{samples:values.length,avg:values.length?values.reduce((a,b)=>a+b,0)/values.length:null,p95:percentile(values,.95),p99:percentile(values,.99),max:values.length?Math.max(...values):null};};
export function createRuntimeProfiler({PerformanceObserverCtor=globalThis.PerformanceObserver,performanceRef=globalThis.performance,capacity=256}={}){
 const metrics=new Map(),longTasks=new Ring(capacity),eventDelay=new Ring(capacity),workerLatency=new Ring(capacity);let observer=null,measures=0,errors=0;
 const metric=name=>{let ring=metrics.get(name);if(!ring){ring=new Ring(capacity);metrics.set(name,ring);}return ring;};
 try{if(PerformanceObserverCtor){observer=new PerformanceObserverCtor(list=>{for(const entry of list.getEntries()){if(entry.entryType==='longtask'||entry.duration>=50)longTasks.push(entry.duration);if(entry.entryType==='event'&&Number.isFinite(entry.duration))eventDelay.push(entry.duration);}});const supported=PerformanceObserverCtor.supportedEntryTypes||[];const types=[];if(supported.includes('longtask'))types.push('longtask');if(supported.includes('event'))types.push('event');for(const type of types)observer.observe({type,buffered:true,durationThreshold:type==='event'?16:undefined});}}catch{observer=null;}
 function record(name,value){metric(String(name)).push(value);return value;}
 function measure(name,fn){const start=performanceRef?.now?.()??Date.now();try{const value=fn();if(value&&typeof value.then==='function')return value.then(result=>{record(name,(performanceRef?.now?.()??Date.now())-start);measures++;return result;},error=>{record(name,(performanceRef?.now?.()??Date.now())-start);measures++;errors++;throw error;});record(name,(performanceRef?.now?.()??Date.now())-start);measures++;return value;}catch(error){record(name,(performanceRef?.now?.()??Date.now())-start);measures++;errors++;throw error;}}
 return{measure,record,recordWorkerLatency(ms){workerLatency.push(ms);},snapshot(){const named={};for(const[name,ring]of metrics)named[name]=summarize(ring);return Object.freeze({measures,errors,longTasks:summarize(longTasks),eventDelay:summarize(eventDelay),workerLatency:summarize(workerLatency),metrics:named});},dispose(){try{observer?.disconnect?.();}catch{}observer=null;}};
}
