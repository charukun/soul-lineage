const PROCESSOR='soul-category-mixer-v1';
const source=`class SoulCategoryMixer extends AudioWorkletProcessor{constructor(options){super();const n=options.processorOptions?.categories||2;this.gains=new Float32Array(n);this.gains.fill(1);this.port.onmessage=e=>{const m=e.data||{};if(m.type==='gain'&&Number.isInteger(m.index)&&m.index>=0&&m.index<this.gains.length)this.gains[m.index]=Math.max(0,Math.min(2,Number(m.value)||0));};}process(inputs,outputs){const out=outputs[0];for(const ch of out)ch.fill(0);for(let i=0;i<inputs.length;i++){const input=inputs[i],gain=this.gains[i]??1;for(let c=0;c<out.length;c++){const src=input[Math.min(c,input.length-1)];if(!src)continue;const dst=out[c];for(let j=0;j<dst.length;j++)dst[j]+=src[j]*gain;}}for(const ch of out)for(let j=0;j<ch.length;j++){const x=ch[j];ch[j]=x/(1+Math.abs(x));}return true;}}registerProcessor('${PROCESSOR}',SoulCategoryMixer);`;
let moduleUrl=null;
export async function createAudioWorkletCategoryMixer(context,{categories=['ambient','effects'],destination=context?.destination}={}){
 if(!context?.audioWorklet?.addModule||typeof globalThis.AudioWorkletNode!=='function')return{supported:false,node:null,connect(source){source?.connect?.(destination);},setGain(){},snapshot:()=>Object.freeze({supported:false,categories:[...categories]}),dispose(){}};
 if(!moduleUrl)moduleUrl=URL.createObjectURL(new Blob([source],{type:'text/javascript'}));await context.audioWorklet.addModule(moduleUrl);const node=new AudioWorkletNode(context,PROCESSOR,{numberOfInputs:categories.length,numberOfOutputs:1,outputChannelCount:[2],processorOptions:{categories:categories.length}});node.connect(destination);let connections=0;return{supported:true,node,connect(source,{category=categories[0]}={}){const raw=categories.indexOf(category),index=raw<0?0:raw;source.connect(node,0,index);connections++;},setGain(category,value){const index=categories.indexOf(category);if(index>=0)node.port.postMessage({type:'gain',index,value});},snapshot(){return Object.freeze({supported:true,categories:[...categories],connections});},dispose(){try{node.disconnect();}catch{}}};}
