export * from './master-character-production.js';
import { createShinoProductionPool as createBasePool } from './master-character-production.js';
import { applyStylizedArtProfile } from './stylized-art.js';
import { applyStylizedShading } from './stylized-shading.js';
import { createAnimationLODScheduler } from './animation-lod.js';
export function productionArtProfileForActorId(id){const value=String(id||'');if(value.includes('Sendagaya_Shino')||value.includes('sendagaya-shino'))return'hero';if(/^review\.[^.]+\.0$/.test(value))return'hero';return'npc';}
function applyProfile(root,profileId){if(!root?.traverse||root.userData?.stylizedArt?.profileId===profileId)return;applyStylizedArtProfile(root,profileId,{cloneMaterials:profileId==='hero'});applyStylizedShading(root,profileId);}
function installAnimationLOD(actor,profileId){if(actor.__stylizedAnimationLOD)return;const scheduler=createAnimationLODScheduler({hero:profileId==='hero'}),sample=actor.sample.bind(actor),updateSecondary=actor.updateSecondary?.bind(actor),setExpressions=actor.setExpressions?.bind(actor),setExpression=actor.setExpression?.bind(actor),reset=actor.reset.bind(actor);let lastTime=null,decision=null,lastCrowdSample=-Infinity;
 actor.sample=(appearance,time=0,pose=null)=>{const dt=lastTime===null?1/60:Math.max(0,Math.min(.25,time-lastTime));lastTime=time;const distance=Math.max(0,Number(actor.root.userData?.presentationDistance)||0),qualityLevel=Math.max(0,Number(actor.root.userData?.visualQualityLevel)||0);let next=scheduler.update(dt,distance,qualityLevel);const crowdHz=profileId==='hero'?0:Math.max(0,Number(actor.root.userData?.crowdAnimationHz)||0);if(crowdHz>0&&crowdHz<next.hz){const crowdDue=time-lastCrowdSample+1e-9>=1/crowdHz,shouldSample=next.shouldSample&&crowdDue;if(shouldSample)lastCrowdSample=time;next={...next,id:`${next.id}-crowd`,hz:crowdHz,shouldSample,expressionsDue:next.expressionsDue&&crowdDue};}decision=next;actor.root.userData.animationLOD={id:decision.id,hz:decision.hz,distance,qualityLevel,secondaryMotion:decision.secondaryMotion,crowdLimited:crowdHz>0&&crowdHz<60};if(decision.shouldSample)sample(appearance,time,pose);};
 if(setExpressions)actor.setExpressions=values=>{if(!decision||decision.expressionsDue)setExpressions(values);};if(setExpression)actor.setExpression=(name,value)=>{if(!decision||decision.expressionsDue)setExpression(name,value);};if(updateSecondary)actor.updateSecondary=(deltaSeconds,enabled=true)=>{if(!decision)return updateSecondary(deltaSeconds,enabled);if(!decision.secondaryMotion)return decision.changed||decision.shouldSample?updateSecondary(Math.max(deltaSeconds,decision.sampleDelta),false):undefined;if(decision.shouldSample)return updateSecondary(Math.max(deltaSeconds,decision.sampleDelta),enabled);};actor.reset=()=>{scheduler.reset();lastTime=null;decision=null;lastCrowdSample=-Infinity;reset();};actor.__stylizedAnimationLOD=true;}
export function createShinoProductionPool(options){const pool=createBasePool(options),spawn=pool.spawn.bind(pool);pool.spawn=(id,...args)=>{const actor=spawn(id,...args),profileId=productionArtProfileForActorId(id);applyProfile(actor.root,profileId);applyProfile(actor.attachments,profileId);installAnimationLOD(actor,profileId);actor.root.userData.stylizedCharacterRole=profileId;if(actor.attachments)actor.attachments.userData.stylizedCharacterRole=profileId;return actor;};return pool;}
